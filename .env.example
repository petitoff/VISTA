# VISTA Configuration
# Copy this file to .env and customize

# Path to your video folder (absolute path on host)
VIDEOS_PATH=/home/petitoff/Videos

# Ports
FRONTEND_PORT=8500
BACKEND_PORT=8501

# Cache settings
CACHE_MAX_SIZE_MB=2048

# ============================================
# NOTE: CVAT and Jenkins credentials are now
# configured via the Settings UI in VISTA
# (click the gear icon in the header)
# ============================================

# Database (used by backend/.env)
# DATABASE_URL=postgresql://vista:vista_secret@localhost:5433/vista
